// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);R// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);D// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);@// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);L// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);L// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);@// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);-// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);-// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);-// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);@// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);H// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);_// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);_// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);[// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);]// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);[// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);]// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);[// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);]// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);ç// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);ã// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);P// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);j// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);|// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);|// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);[// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);]// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);L// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);P// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);F// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);á// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);[// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);]// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);+// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);2// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);%// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);6// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);4// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);-// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);-// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);j// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);-// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);H// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);6// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);4// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);H// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);H// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);P// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);L// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);4// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);3// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);4// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);N// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);ç// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);õ// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);?// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);_// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);?// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);?// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);_// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);|// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);|// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);?// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);_// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);3// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);2// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);3// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);2// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);w// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);3// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);2// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);3// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);2// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);?// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);_// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);?// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);.// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);0// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);|// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);|// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);4// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);5// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);-// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);'// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);,// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);E// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);P// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);P// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);ç// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);õ// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);U// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);(// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);H// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);P// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);L// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);D// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);v// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);k// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);L// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);L// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);z// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);:// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);1// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);c// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);"// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);S// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);g// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);y// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);m// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);B// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);A// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);i// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);O// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);=// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);{// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);h// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);C// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);s// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);I// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);n// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);M// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);<// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);/// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);>// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);)// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);}// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);x// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);d// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);e// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);f// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);u// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);l// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);t// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\); // Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);T// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);o// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);p// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);b// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);a// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);r// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);;// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);
// Carregar alertas para o sino de notificação
  useEffect\(\(\) => \{
    const fetchAlerts = async \(\) => \{
      try \{
        // Buscar alertas reais da API
        const response = await fetch(/api/alerts/);
        if \(response\.ok\) \{
          const data = await response\.json\(\); 
          setAlerts\(data\.alerts || \[\]\);
        \} else \{
          setAlerts\(\[\]\);
        \}
      \} catch \(error\) \{
        console\.error\(Erro ao carregar alertas:, error\);
        setAlerts\(\[\]\);
      \}
    \};

    fetchAlerts\(\);